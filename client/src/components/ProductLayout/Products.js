import React from "react";
import { Card, Row, Col } from "react-materialize";
import utils from "../"

const products = [
    {}
];

const Products = () => {
    <main>
        <Col>
        <Card>
            fsfdsfsdf
        </Card>
        </Col>
    </main>
}

export default Products;